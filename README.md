# bourne-again

```sh
if [ ! -d ~/.bash ]; then mkdir ~/.bash; fi
cd ~/.bash
git clone https://github.com/raym/bourne-again.git
echo "source ~/.bash/bourne-again/bashrc.sh" >> ~/.bashrc
echo "source ~/.bash/bourne-again/profile.sh" >> ~/.profile
```
